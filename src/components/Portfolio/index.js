import PropTypes from 'prop-types';

import './style.scss';

const Portfolio = () => {
  console.log('portfolio');
  return (
    <section>
      portfolio
    </section>
  );
};

Portfolio.propTypes = {

};

export default Portfolio;
